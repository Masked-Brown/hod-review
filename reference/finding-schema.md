<!-- STUB · scaffolded by M1 (skeleton). Owner: M2. No substance until then. -->

# finding-schema.md

**Status:** stub — not yet written.
**Owner:** manifest **M2**.
**Purpose:** the exact finding shape from `spec.md` — the fields
`SLIDE`, `QUOTE`, `PRINCIPLE`, `SPEC`, `WHY`, `QUESTION` — as the editor emits them
and as `check.py` parses them. One file, one job: the contract shared by the editor
and the gate.

M1 records the field names for shape only; the schema definition is authored in M2.
See `spec.md` (The finding schema) and `plan.md` §3 (M2).
