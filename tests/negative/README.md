<!-- STUB · scaffolded by M1 (skeleton). Owner: M3. -->

# tests/negative/

**Status:** stub directory — no cases yet.
**Owner:** manifest **M3**.
**Purpose:** deliberately broken critiques that must each fail on a *named* check —
verify the verifier. Planned set (from `plan.md` §3, M3):

- one that quotes a line never on any slide (fabrication check);
- one that refuses to rewrite then rewrites inside a field;
- one that invents a finding on a clean deck;
- one generic-feedback finding with no anchor.

The self-test asserts N/N known-bad are rejected. "A checker that passes everything
proves nothing." See `plan.md` §3 (M3).
