<!-- STUB · scaffolded by M1 (skeleton). Owner: M3 (populated with M4 outputs). -->

# tests/cases/

**Status:** stub directory — no cases yet.
**Owner:** manifest **M3** (harness), drawing on the constructed **M4** run.
**Purpose:** the shipped example critiques that `tests/verify.py` runs the gate
against, asserting each clears the contract. These are the "clears every clean"
half of the self-test.

See `plan.md` §3 (M3).
