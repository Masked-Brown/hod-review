<!-- STUB · scaffolded by M1 (skeleton). Owner: M5, and only if built (optional). -->

# docs/

**Status:** stub — optional, may stay empty.
**Owner:** manifest **M5**, optional.
**Purpose:** a demonstration surface only — secondary, and never the thing a judge
drops into a Claude project. The editor is the folder, not a site.

This directory exists in the skeleton to make the boundary explicit: if a demo surface
is ever built, it lives here and stays secondary. `waypoint` is the field's cautionary
tale — a full app that buried the actual editor. See `communitycompetitions.md` and
`plan.md` §7 (website dilution).
