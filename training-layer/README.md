<!-- STUB · scaffolded by M1 (skeleton). Owner: M2.5 (schema + one ILLUSTRATIVE row). -->

# training-layer/

**Status:** stub — schema not yet written.
**Owner:** manifest **M2.5** defines the schema and ships exactly one `ILLUSTRATIVE`
row; manifest **M4** adds the `CONSTRUCTED` rows and the accumulated teacher file.
**Purpose:** the accretion mechanism, shipped populated (never empty, never
placeholder-filled). Two artifacts:

- this `README.md` — the five-column run-level table schema (Input · Editor reasoning ·
  Finding and rationale · Training-layer impact · Future-sample benefit) and one row
  marked `ILLUSTRATIVE`;
- `<teacher-id>.md` — the distilled recurring patterns for that (constructed) teacher,
  carried across runs, written in M4.

Row labels are never blurred: `ILLUSTRATIVE` (M2.5, one row), `CONSTRUCTED` (M4, this
build's evidence), `REAL` (reserved for a genuine teacher run, unused here and named
as a next step in `OPEN-DEFECTS.md`). See `plan.md` §5.
